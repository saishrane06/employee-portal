Include:

Default bridge network.
Custom bridge network.
Why container names are used instead of IPs.
Commands:
docker network create
docker network ls
docker network inspect
docker run --network ...
